import { Box } from "@chakra-ui/react";
import React from "react";

function UserWrapper(props) {
  const { pageName } = props;
  if(pageName){
    if(pageName){
    
    }
  }
  
  return <Box as="section">{pageName}</Box>;
}
export default UserWrapper;
